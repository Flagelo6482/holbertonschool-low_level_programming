#include <stdio.h>

/**
 * main - Ejecutamos los numeros del 0 al 10.
 * Return: Retornamos 0.
 */
int main(void)
{
	int a = 0;

	while (a < 10)
	{
		printf("%d", a);
		a++;
	}
	printf("\n");
	return (0);
}
