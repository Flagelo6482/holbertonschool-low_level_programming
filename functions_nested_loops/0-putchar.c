#include "main.h"
/**
 *main - Entry point
 *
 *Return: Always 0
 */
int main(void)
{
	int x = 0;

	char c[] = "_putchar";

	while (x <= c[x] && c[x] != '\0')
	{
		_putchar(c[x]);
		x++;
	}

	_putchar(10);

	return (0);
}
