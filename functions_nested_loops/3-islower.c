#include "main.h"

/**
 * islower - Write a function that checks for lowercase character.
 * @c: Task 1
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		_putchar(c);
	}
	else
	{
		_putchar(c);
	}
	_putchar('\n');
	return (0);
}
